[project]
name = "py-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "Gharantes@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
py-api = "py_api:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
